{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "prepare": "husky install",
        "pre-commit": "composer format && composer analyse",
        "pre-push": "composer test"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "husky": "^8.0.3",
        "laravel-vite-plugin": "^0.7.2",
        "lint-staged": "^13.2.0",
        "vite": "^4.0.0"
    }
}
